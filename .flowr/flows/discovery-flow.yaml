flow: discovery-flow
version: 12.0.0
exits:
  - complete

states:
  - id: stakeholder-interview
    attrs:
      description: "PO interviews stakeholders to understand pain points, business goals, and domain terms"
      owner: PO
      git: dev
      skills:
        - conduct-interview
      in:
        - ".cache/interview-notes/*.md"  # optional
        - "domain_spec.md"  # optional — for re-interview context
        - "product_definition.md"  # optional — for re-interview context
      out:
        - ".cache/interview-notes/<session_id>.md"
    next:
      needs-spec: spec-creation
      already-known: complete

  - id: spec-creation
    attrs:
      description: "SA and DE create a structural domain specification per bounded context from interview notes"
      owner: SA
      git: dev
      skills:
        - create-domain-spec
        - define-ubiquitous-language
      in:
        - .cache/interview-notes/*.md
        - "domain_spec.md"  # optional — cumulative edit across iterations
        - "glossary.md"  # optional — cumulative edit across iterations
        - "product_definition.md"  # optional — cumulative edit across iterations
      out:
        - product_definition.md:
            - what_is
            - what_is_not
            - why
            - users
            - out_of_scope
            - quality_attributes
        - domain_spec.md:
            - context_map
            - context
            - entities
            - relationships
            - aggregate_boundaries
            - data_shapes
            - integration_points
            - external_contracts
        - glossary.md
    conditions:
      committed-to-dev-locally:
        committed-to-dev-locally: ==verified
    next:
      done:
        to: complete
        when: committed-to-dev-locally
      needs-reinterview: stakeholder-interview
