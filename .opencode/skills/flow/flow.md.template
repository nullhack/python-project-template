# FLOW — <Workflow Name>

This file is a redirect. The static state machine definition lives in `.flowr/flows/`.
The dynamic session tracking lives in `.flowr/sessions/`.

For the full flow specification, see `.flowr/flows/feature-flow.yaml`.

For session protocol, load `skill run-session`.