# WORK — Active Work Tracking

This file tracks live work items. The workflow rules live in `FLOW.md`.

Each item carries exactly the variables defined by `FLOW.md`:
- `@id` — <description from FLOW.md>
- `@state` — current state in the workflow
- `@branch` — <description from FLOW.md>

<!-- Add or remove variable descriptions to match your FLOW.md Work Variables section -->

---

## Active Items

<!-- One entry per in-flight work item. Remove when item reaches terminal state.
Format:
- @id: <value>
  @state: <value>
  @branch: <value>
-->

*(no active items)*

